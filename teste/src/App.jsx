function ExerciseList() {
  var r = ""

  const solution = (str, ending, r) => {
    // TODO: complete
     srt = "abc";
     ending = "bc"
    const c = "d"
    r = "r"
    if(srt.slice(2) == ending) {
      return r = "sim"
    } else {
      return r = "nao"
    }
  }

  console.log(r)

  return (
    <div>
      ...
    </div>
  );
}

export default ExerciseList;